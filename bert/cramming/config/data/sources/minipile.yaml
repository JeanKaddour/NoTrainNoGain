# The minipile dataset, drawn from it huggingface mirror
JeanKaddour/minipile:
  provider: huggingface
  partition: null
  split: train

  streaming: False

  # source-specific cleaning rules?
  remove_columns:
  concatenate_successive_entries: 0
